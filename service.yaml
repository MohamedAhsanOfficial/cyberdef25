apiVersion: v1
kind: Service
metadata:
  name: malware-detector
  labels:
    app: malware-detector
spec:
  type: ClusterIP
  selector:
    app: malware-detector
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
